{
  "id": "memory-bridge",
  "name": "Memory Bridge (FlipClaw)",
  "version": "1.0.0",
  "description": "FlipClaw — Post-turn memory capture. Fires after every agent turn and runs incremental-memory-capture.py to extract durable facts into daily logs and session cache. Part of FlipClaw; not a stock OpenClaw feature.",
  "entry": "./index.ts",
  "configSchema": {
    "type": "object",
    "additionalProperties": true,
    "properties": {
      "enabled": { "type": "boolean", "default": true }
    }
  }
}
