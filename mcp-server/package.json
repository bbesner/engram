{
  "name": "flipclaw-memory-mcp",
  "version": "1.0.0",
  "description": "FlipClaw MCP server — exposes an OpenClaw agent's shared memory (MEMORY.md, memory/*, skills/) to a remote Claude Code CLI over stdio. Ships as part of FlipClaw.",
  "main": "server.mjs",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
